import { useEffect, useState } from 'react'
import { AlertTriangle, Copy, Eye, EyeOff } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { Alert } from '@/components/ui/alert'
import { SectionCard, ReadOnlyField } from './fields'
import { apiFetch, APIError } from '../../../api/client'

const kinds = ['serviceRoleKey', 'jwtSecret', 'databasePassword', 'anonKey'] as const
type Kind = typeof kinds[number]
export function SecretsSection({ projectId, projectUrl, anonKey }: { projectId: string; projectUrl: string; anonKey: string }) {
  const [values, setValues] = useState<Partial<Record<Kind, string>>>({}); const [open, setOpen] = useState(false); const [password, setPassword] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState<Kind | undefined>()
  useEffect(() => () => setValues({}), [])
  const reveal = async (kind: Kind) => { setBusy(kind); setError(''); try { const result = await apiFetch<{ value: string }>(`/api/projects/${projectId}/secrets/${kind}/reveal`, { method: 'POST', body: JSON.stringify({ password }) }); setValues((current) => ({ ...current, [kind]: result.value })); window.setTimeout(() => setValues((current) => { const next = { ...current }; delete next[kind]; return next }), 60_000) } catch (caught) { setError(caught instanceof APIError ? caught.message : 'Unable to reveal secret') } finally { setBusy(undefined) } }
  const copy = async (kind: Kind) => { if (values[kind]) await navigator.clipboard?.writeText(values[kind] as string) }
  return <SectionCard title="API & Secrets" description="Project URL and Anon Key are safe client outputs. Sensitive values require recent administrator authentication."><ReadOnlyField label="Project URL" value={projectUrl || 'Unavailable'} copy={Boolean(projectUrl)} /><ReadOnlyField label="Anon Key" value={anonKey || 'Unavailable'} copy={Boolean(anonKey)} /><div className="rounded-lg border border-border p-4"><div className="flex items-center gap-2"><AlertTriangle className="size-4 text-amber-500" /><strong className="text-sm">Sensitive values</strong></div><p className="mt-2 text-sm text-muted-foreground">Values stay in memory only and are cleared when leaving this section or after 60 seconds.</p><Button type="button" variant="outline" className="mt-3" onClick={() => { setOpen((current) => !current); setPassword(''); setError('') }}><Eye className="size-4" />{open ? 'Hide reveal controls' : 'Reveal sensitive values'}</Button>{open && <div className="mt-4 space-y-3"><label htmlFor="reveal-password" className="text-sm font-medium">Administrator password</label><input id="reveal-password" type="password" autoComplete="current-password" className="h-9 w-full rounded-md border bg-background px-3 text-sm" value={password} onChange={(event) => setPassword(event.target.value)} />{error && <Alert variant="destructive">{error}</Alert>}{kinds.map((kind) => <div key={kind} className="flex items-center justify-between gap-2"><span className="text-sm">{kind}</span>{values[kind] ? <><code className="max-w-[260px] truncate">{values[kind]}</code><Button type="button" size="icon" variant="outline" aria-label={`Copy ${kind}`} onClick={() => void copy(kind)}><Copy /></Button><Button type="button" size="icon" variant="outline" aria-label={`Hide ${kind}`} onClick={() => setValues((current) => { const next = { ...current }; delete next[kind]; return next })}><EyeOff /></Button></> : <Button type="button" size="sm" variant="outline" disabled={!password || Boolean(busy)} onClick={() => void reveal(kind)}>{busy === kind ? 'Revealing…' : 'Reveal'}</Button>}</div>)}</div>}</div></SectionCard>
}

