import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'
import { RotateCw } from 'lucide-react'
import { useEffect, useState } from 'react'
import { useNavigate, useParams, useSearchParams } from 'react-router-dom'
import { toast } from 'sonner'
import { Alert } from '@/components/ui/alert'
import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle } from '@/components/ui/alert-dialog'
import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Tabs, TabsList, TabsTrigger } from '@/components/ui/tabs'
import { APIError, apiFetch } from '../../../api/client'
import type { AuthConfig, DatabaseConfig, FunctionsConfig, GeneralConfig, NetworkConfig, OAuthProviderConfig, PoolerConfig, RealtimeConfig, RedactedProjectConfiguration, Services, SMTPConfig, StorageConfig } from '../../../api/types'
import { OperationPanel } from '../../operations/OperationPanel'
import { AuthSection } from './AuthSection'
import { DatabaseSection } from './DatabaseSection'
import { FunctionsSection } from './FunctionsSection'
import { GeneralSection } from './GeneralSection'
import { NetworkSection } from './NetworkSection'
import { OAuthSection } from './OAuthSection'
import { PoolerSection } from './PoolerSection'
import { RealtimeSection } from './RealtimeSection'
import { SecretsSection } from './SecretsSection'
import { ServicesSection } from './ServicesSection'
import { SMTPSection } from './SMTPSection'
import { StorageSection } from './StorageSection'
import { affectedServices, CONFIGURATION_SECTIONS, dirtyLabels, sectionImpact, sectionLabel, type ConfigurationSection, type PendingConfigurationSave } from './types'
import { useConfigurationMutation } from './useConfigurationMutation'

type Snapshot = { projectId: string; revision: number; lastGoodRevision: number; configuration: RedactedProjectConfiguration; projectUrl?: string; anonKey?: string }

export function ConfigurationPage() {
  const { projectId = '' } = useParams(); const [params] = useSearchParams(); const navigate = useNavigate(); const queryClient = useQueryClient()
  const requested = params.get('section') ?? 'general'; const section: ConfigurationSection = CONFIGURATION_SECTIONS.includes(requested as ConfigurationSection) ? requested as ConfigurationSection : 'general'
  const configuration = useQuery({ queryKey: ['project-configuration', projectId], queryFn: () => apiFetch<Snapshot>(`/api/projects/${projectId}/configuration`), enabled: Boolean(projectId) })
  const [pending, setPending] = useState<PendingConfigurationSave>(); const [operation, setOperation] = useState<{ projectId: string; operationId: string }>(); const [conflict, setConflict] = useState(false); const [rotateOpen, setRotateOpen] = useState(false); const [rotatePassword, setRotatePassword] = useState('')
  const update = useConfigurationMutation(projectId, configuration.data?.revision ?? 0, (result) => { setPending(undefined); setOperation({ projectId: result.projectId, operationId: result.operationId }); setConflict(false); toast.success('Configuration update queued') }, (error) => { if (error instanceof APIError && error.status === 409) setConflict(true); toast.error(error.message) })
  const rotate = useMutation({ mutationFn: () => apiFetch<{ projectId: string; operationId: string }>(`/api/projects/${projectId}/secrets/databasePassword/rotate`, { method: 'POST', body: JSON.stringify({ password: rotatePassword }) }), onSuccess: (result) => { setRotateOpen(false); setRotatePassword(''); setOperation(result); toast.success('Database password rotation queued') }, onError: (error: Error) => toast.error(error.message) })
  useEffect(() => { if (requested !== section) navigate(`/projects/${projectId}/configuration?section=${section}`, { replace: true }) }, [navigate, projectId, requested, section])
  const submit = (value: unknown, dirty: unknown, provider?: string) => { const labels = dirtyLabels(dirty).map((name) => name.replaceAll('.', ' → ')); if (labels.length) setPending({ section, provider, value, labels, services: affectedServices(section, dirty), impact: sectionImpact(section, value) }) }
  const reload = async () => { setConflict(false); setPending(undefined); await configuration.refetch() }
  const completed = async () => { await Promise.all([queryClient.invalidateQueries({ queryKey: ['project-configuration', projectId] }), queryClient.invalidateQueries({ queryKey: ['project', projectId] })]); setOperation(undefined) }
  if (configuration.isLoading) return <main className="page"><div className="empty-state">Loading configuration…</div></main>
  if (configuration.error || !configuration.data) return <main className="page"><Alert variant="destructive">Unable to load project configuration.</Alert></main>
  const config = configuration.data.configuration
  if (operation) return <main className="page configuration-page"><div className="page-heading"><div><p className="eyebrow">Configuration operation</p><h1>Applying project configuration</h1><p className="muted">The runtime is reconciled in the background. This panel remains available until completion.</p></div></div><OperationPanel operationId={operation.operationId} projectId={projectId} projectName={projectId} onSucceeded={completed} /></main>
  const save = (value: unknown, dirty: unknown, provider?: string) => submit(value, dirty, provider)
  return <main className="page configuration-page"><div className="page-heading"><div><p className="eyebrow">Installed project</p><h1>Configuration</h1><p className="muted">Typed settings for this project. Secrets remain encrypted and redacted.</p></div><Badge variant="outline">Revision {configuration.data.revision}</Badge></div>{conflict && <Alert variant="destructive" className="mb-4"><div className="flex items-center justify-between gap-3"><span>This configuration is stale. Your dirty fields are preserved.</span><Button size="sm" variant="outline" onClick={() => void reload()}>Reload</Button></div></Alert>}<Tabs value={section} onValueChange={(value) => navigate(`/projects/${projectId}/configuration?section=${value}`)}><TabsList className="grid w-full grid-cols-3 gap-1 md:grid-cols-6 xl:grid-cols-12">{CONFIGURATION_SECTIONS.map((name) => <TabsTrigger key={name} value={name}>{sectionLabel(name)}</TabsTrigger>)}</TabsList></Tabs><div className="mt-4">{section === 'general' && <GeneralSection initial={config.general} onSave={save as (value: GeneralConfig, dirty: unknown) => void} />}{section === 'services' && <ServicesSection initial={config.services} onSave={save as (value: Services, dirty: unknown) => void} />}{section === 'auth' && <AuthSection initial={config.auth as AuthConfig} onSave={save as (value: AuthConfig, dirty: unknown) => void} />}{section === 'smtp' && <SMTPSection initial={config.auth.smtp as SMTPConfig} onSave={save as (value: SMTPConfig, dirty: unknown) => void} />}{section === 'oauth' && <OAuthSection initial={config.auth.oauth as Record<string, OAuthProviderConfig>} general={config.general} onSave={(provider, value, dirty) => save(value, dirty, provider)} />}{section === 'storage' && <StorageSection initial={config.storage as StorageConfig} storageEnabled={config.services.storage} onSave={save as (value: StorageConfig, dirty: unknown) => void} />}{section === 'realtime' && <RealtimeSection initial={config.realtime} enabled={config.services.realtime} onSave={save as (value: RealtimeConfig, dirty: unknown) => void} />}{section === 'functions' && <FunctionsSection initial={config.functions as FunctionsConfig} enabled={config.services.functions} onSave={save as (value: FunctionsConfig, dirty: unknown) => void} />}{section === 'database' && <DatabaseSection initial={config.database as DatabaseConfig} directDb={config.services.directDb} onSave={save as (value: DatabaseConfig, dirty: unknown) => void} onRotate={() => setRotateOpen(true)} />}{section === 'pooler' && <PoolerSection initial={config.pooler as PoolerConfig} enabled={config.services.supavisor} onSave={save as (value: PoolerConfig, dirty: unknown) => void} />}{section === 'network' && <NetworkSection initial={config.network as NetworkConfig} onSave={save as (value: NetworkConfig, dirty: unknown) => void} />}{section === 'secrets' && <SecretsSection projectId={projectId} projectUrl={configuration.data.projectUrl ?? (config.general.domain ? `https://${config.general.domain}` : '')} anonKey={configuration.data.anonKey ?? ''} />}</div><AlertDialog open={Boolean(pending)} onOpenChange={(open) => !open && setPending(undefined)}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Apply configuration changes?</AlertDialogTitle><AlertDialogDescription>Only the dirty fields in {pending && sectionLabel(pending.section)} will be sent.</AlertDialogDescription></AlertDialogHeader>{pending && <div className="space-y-3 text-sm"><div><strong>Changed settings</strong><ul className="mt-1 list-disc pl-5">{pending.labels.map((label) => <li key={label}>{label}</li>)}</ul></div><div><strong>Affected services</strong><p className="mt-1 text-muted-foreground">{pending.services.join(', ') || 'Configuration metadata only'}</p></div><Badge variant="outline">{pending.impact === 'recreate' ? 'Runtime recreate required' : pending.impact === 'restart' ? 'Service restart required' : 'No runtime restart expected'}</Badge></div>}<AlertDialogFooter><AlertDialogCancel>Keep editing</AlertDialogCancel><AlertDialogAction onClick={() => pending && update.mutate(pending)}>Confirm and apply</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog><AlertDialog open={rotateOpen} onOpenChange={setRotateOpen}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Rotate database password?</AlertDialogTitle><AlertDialogDescription>Existing database clients will need the new password after reconciliation. This action is audited and cannot be undone.</AlertDialogDescription></AlertDialogHeader><label htmlFor="rotate-admin-password" className="text-sm font-medium">Administrator password</label><input id="rotate-admin-password" type="password" autoComplete="current-password" className="h-9 w-full rounded-md border bg-background px-3" value={rotatePassword} onChange={(event) => setRotatePassword(event.target.value)} /><AlertDialogFooter><AlertDialogCancel>Cancel</AlertDialogCancel><AlertDialogAction disabled={!rotatePassword || rotate.isPending} onClick={() => rotate.mutate()}><RotateCw className="size-4" />Confirm rotation</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></main>
}
