import type { UseFormReturn } from 'react-hook-form'
import { Alert } from '@/components/ui/alert'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { Field, FieldDescription, FieldError, FieldGroup, FieldLabel } from '@/components/ui/field'
import { Input } from '@/components/ui/input'
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select'
import { Switch } from '@/components/ui/switch'
import { Textarea } from '@/components/ui/textarea'
import type { ProjectForm } from './projectSchema'
import { setServiceEnabled } from './PresetStep'

export function StorageFunctionsStep({ form }: { form: UseFormReturn<ProjectForm> }) {
  const storage = form.watch('configuration.storage'); const services = form.watch('configuration.services'); const functions = form.watch('configuration.functions')
  const set = (name: string, value: unknown) => { form.setValue(name as any, value as any, { shouldDirty: true, shouldValidate: true }); form.setValue('preset', 'CUSTOM', { shouldDirty: true }) }
  const error = (name: string) => { let value: any = form.formState.errors; for (const part of name.split('.')) value = value?.[part]; return (value?.message || value?.value?.message) as string | undefined }
  const setBackend = (backend: string) => { const base = form.getValues('configuration.storage'); const clean = backend === 'local' ? { ...base, backend, bucket: '', region: '', endpoint: '', accountId: '', accessKeyId: '', secretAccessKeySet: false, secretAccessKey: { action: '' as const }, forcePathStyle: false } : { ...base, backend, region: backend === 'r2' ? '' : base.region, endpoint: backend === 's3' ? base.endpoint : '', accountId: backend === 'r2' ? base.accountId : '', forcePathStyle: backend === 'r2' ? true : base.forcePathStyle }; set('configuration.storage', clean) }
  const variablesText = functions.variables.map((variable) => `${variable.name}=`).join('\n'); const variableErrors = (form.formState.errors.configuration?.functions?.variables as any[] | undefined) ?? []
  return <Card><CardHeader><CardTitle>Storage & Functions</CardTitle><CardDescription>Backend credentials are write-only; service changes also reconcile their dependencies.</CardDescription></CardHeader><CardContent className="space-y-7"><Field orientation="horizontal" className="items-center justify-between rounded-lg border border-border px-3 py-2"><FieldLabel>Storage service</FieldLabel><Switch checked={services.storage} onCheckedChange={(enabled) => setServiceEnabled(form, 'storage', enabled)} /></Field><Field orientation="horizontal" className="items-center justify-between rounded-lg border border-border px-3 py-2"><FieldLabel>S3-compatible API</FieldLabel><Switch checked={storage.s3CompatibleApi} onCheckedChange={(enabled) => set('configuration.storage.s3CompatibleApi', enabled)} /></Field>{services.storage && <section className="space-y-4"><Field><FieldLabel>Storage backend</FieldLabel><Select value={storage.backend} onValueChange={(value) => value && setBackend(value)}><SelectTrigger aria-label="Storage backend"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="local">Local filesystem</SelectItem><SelectItem value="aws-s3">AWS S3</SelectItem><SelectItem value="r2">Cloudflare R2</SelectItem><SelectItem value="s3">Custom S3-compatible</SelectItem></SelectContent></Select><FieldError>{error('configuration.storage.backend')}</FieldError></Field>{storage.backend === 'local' ? <Alert>Local storage uses the managed path <code>{storage.localPath || './volumes/storage'}</code>.</Alert> : <><FieldGroup className="grid gap-4 md:grid-cols-2"><TextField label="Bucket" name="configuration.storage.bucket" form={form} error={error('configuration.storage.bucket')} /><TextField label="Region" name="configuration.storage.region" form={form} error={error('configuration.storage.region')} />{storage.backend === 'r2' ? <TextField label="Account ID" name="configuration.storage.accountId" form={form} error={error('configuration.storage.accountId')} /> : <TextField label="Endpoint" name="configuration.storage.endpoint" form={form} error={error('configuration.storage.endpoint')} />}<TextField label="Access key ID" name="configuration.storage.accessKeyId" form={form} error={error('configuration.storage.accessKeyId')} /><Field><FieldLabel htmlFor="storage-secret">Secret access key</FieldLabel><Input id="storage-secret" type="password" onChange={(event) => set('configuration.storage.secretAccessKey', event.target.value ? { action: 'replace', value: event.target.value } : { action: '' })} /><FieldError>{error('configuration.storage.secretAccessKey')}</FieldError></Field></FieldGroup><Field><FieldLabel htmlFor="storage-upload-limit">Upload limit (MiB)</FieldLabel><Input id="storage-upload-limit" type="number" min={1} max={5120} value={Math.round(storage.uploadFileSizeLimit / (1024 * 1024))} onChange={(event) => { const mib = Number(event.target.value); if (Number.isInteger(mib) && mib >= 1 && mib <= 5120) set('configuration.storage.uploadFileSizeLimit', mib * 1024 * 1024) }} /></Field>{storage.backend !== 'r2' && <Field orientation="horizontal"><FieldLabel>Force path style</FieldLabel><Switch checked={storage.forcePathStyle} onCheckedChange={(enabled) => set('configuration.storage.forcePathStyle', enabled)} /></Field>}</>}</section>}<section className="space-y-4"><div className="flex items-center justify-between"><div><h3 className="font-medium">Edge Functions</h3></div><Switch aria-label="Enable Functions" checked={services.functions} onCheckedChange={(enabled) => setServiceEnabled(form, 'functions', enabled)} /></div>{services.functions && <Field><FieldLabel htmlFor="function-variables">Environment variables</FieldLabel><FieldDescription>Enter NAME=value lines.</FieldDescription><Textarea id="function-variables" value={variablesText} onChange={(event) => set('configuration.functions.variables', event.target.value.split(/\n/).filter(Boolean).map((line) => { const index = line.indexOf('='); const name = (index < 0 ? line : line.slice(0, index)).trim(); const value = index < 0 ? '' : line.slice(index + 1); const existing = functions.variables.find((variable) => variable.name === name); return { name, valueSet: value ? true : !!existing?.valueSet, value: value ? { action: 'replace' as const, value } : existing?.valueSet ? existing.value : { action: '' as const } } }))} />{variableErrors.map((item, index) => <FieldError key={index}>{item?.name?.message || item?.value?.message}</FieldError>)}</Field>}</section></CardContent></Card>
}
function TextField({ label, name, form, error }: { label: string; name: string; form: UseFormReturn<ProjectForm>; error?: string }) { return <Field><FieldLabel htmlFor={name}>{label}</FieldLabel><Input id={name} {...form.register(name as any)} aria-invalid={!!error} /><FieldError>{error}</FieldError></Field> }
